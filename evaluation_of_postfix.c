#include<stdio.h>
#include<onio.h>
int main()
{ 
  char post[50];
  printf("enter a postfix expression\n");
  gets(post);
  evapos(post);
  getch();
  return 0;
  
}
void evapos(char postx[])
{ int l,i,x=0,y=0;
l=strlen(postx);
for(i=0;i<l;i++)
{ switch(postX[i])
{ 
}
}
}
